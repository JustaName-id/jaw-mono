/**
 * JustaName SDK is not available in React Native
 * This is a placeholder to prevent import errors
 *
 * Address resolution will be skipped in React Native and truncated addresses will be shown
 */

/**
 * Get JustaName SDK instance - NOT SUPPORTED IN REACT NATIVE
 *
 * @returns Always throws error in React Native
 * @throws Error indicating JustaName SDK is not available
 */
export function getJustaNameInstance(): never {
  throw new Error('JustaName SDK is not available in React Native environment');
}
