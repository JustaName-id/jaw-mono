{
  "name": "@jaw-mono/demo",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/demo/app",
  "projectType": "application",
  "tags": ["npm:private"],
  "targets": {
    "dev": {
      "continuous": true,
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "if [ -f ../playground/certificates/localhost-key.pem ]; then bunx next dev --experimental-https --experimental-https-key ../playground/certificates/localhost-key.pem --experimental-https-cert ../playground/certificates/localhost.pem; else bunx next dev --experimental-https; fi"
      }
    }
  }
}
